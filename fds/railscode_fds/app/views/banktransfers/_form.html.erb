<%= form_for(banktransfer) do |f| %>
  <% if banktransfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(banktransfer.errors.count, "error") %> prohibited this banktransfer from being saved:</h2>

      <ul>
      <% banktransfer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
